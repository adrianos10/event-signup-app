import { NextPage } from 'next';

const Home: NextPage = () => (
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100%',
    }}>
    next-app-template
  </div>
);

export default Home;
